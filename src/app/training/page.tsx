import * as React from "react";
import { Archive } from "lucide-react";

type Props = {};

export default function trainingPage(props: Props) {
  return (
    <div className="flex justify-start items-center gap-3">
      <Archive /> Training Page
    </div>
  );
}
